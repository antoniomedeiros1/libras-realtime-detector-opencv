# libras-realtime-detector

## Instruções

Executar o comando a seguir para fazer a instalção das bibliotecas (de preferência utilizando virtualenv):

```sh
pip install -r requirements.txt
```

Para executar o app basta rodar:

```sh
python -m opencvapp
```
